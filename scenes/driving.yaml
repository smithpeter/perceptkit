id: driving
version: 1
describe:
  template: "驾驶场景"
match:
  all:
    - { feature: audio.rms_db, op: gt, value: -30.0 }
  any:
    - { feature: context.motion, op: eq, value: vehicle }
    - { feature: audio.noise_type, op: eq, value: road }
priority: 15
